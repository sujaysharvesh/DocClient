export interface User {
    id: string;
    name: string;
    color: string;
    avatar: string;
    isOwner: boolean;
}

export interface UserData {
    id: string;
    username: string;
    email: string;
}
